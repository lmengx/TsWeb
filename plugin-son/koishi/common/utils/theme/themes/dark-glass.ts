import type { Theme } from '../tokens'

/**
 * 深夜玻璃（默认）：改造前的视觉基准，令牌差异为空。
 * 基准值见 tokens.ts 的 BASE_TOKENS。
 */
const theme: Theme = {
  id: 'dark-glass',
  name: '深夜玻璃',
  description: '深紫渐变画布 + 半透明磨砂卡面（默认，改造前样式）',
  version: 1,
  tokens: {},
}

export default theme
